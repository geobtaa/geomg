<h1 style="width:100%;">
  Imports
  <%= link_to '+ New Import', new_import_path, { class: 'btn btn-primary float-right' } %>
</h1>

<h6>
  <span class='float-left mt-3'>
    <%== pagy_info(@pagy, 'imports'.pluralize(@pagy.count)) %>
  </span>
  <span class='float-right'>
    <%== pagy_bootstrap_nav(@pagy) %>
  </span>
</h6>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Filename</th>
      <th>Source</th>
      <th>Description</th>
      <th>Row count</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @imports.each do |import| %>
      <tr>
        <td><%= import.name %></td>
        <td><%= import.filename %></td>
        <td><%= import.source %></td>
        <td><%= import.description %></td>
        <td><%= import.row_count %></td>
        <td><%= import.created_at %></td>
        <td><%= link_to 'Show', import_path(import) %></td>
        <td><%= link_to 'Edit', edit_import_path(import) %></td>
        <td><%= link_to 'Destroy', import_path(import), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h6>
  <span class='float-left mt-3'>
    <%== pagy_info(@pagy, 'imports'.pluralize(@pagy.count)) %>
  </span>
  <span class='float-right'>
    <%== pagy_bootstrap_nav(@pagy) %>
  </span>
</h6>

<br/>
